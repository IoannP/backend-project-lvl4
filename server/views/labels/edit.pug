extends ../layouts/application.pug

block header
  = t('views.label.edit.edit')

block content
  +formFor()(action=`/labels/${label.id}` method="post")
    +input(label, 'name', t('views.label.edit.name'), errors)(type="text")
    input(name="_method" type="hidden" value="patch")
    +submit()(value=t('views.label.edit.submit'))