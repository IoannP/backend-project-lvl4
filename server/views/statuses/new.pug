extends ../layouts/application.pug

block header
  = t('views.status.new.create')

block content
  +formFor()(action=route('statuses') method='post')
    +input(status, 'name', t('views.status.new.name'), errors)(type="text")
    +submit(user)(value=t('views.status.new.submit'))