extends ../layouts/application.pug

block content
  a(href=route('newTask'))= t('views.task.list.create')
  include ./filter.pug
  table.table.table-hover
    thead
      tr
        th(scope='col')= t('views.task.list.id')
        th(scope='col')= t('views.task.list.name')
        th(scope='col')= t('views.task.list.status')
        th(scope='col')= t('views.task.list.author')
        th(scope='col')= t('views.task.list.performer')
        th(scope='col')= t('views.task.list.dateCreated')
        th
    tbody
      each task in tasks
        tr
          td= task.id
          td
            a(href=`/tasks/${task.id}`)= task.name
          td= task.status
          td= task.author
          td= task.performer
          td= formatDate(task.createdAt)
          td
            a(href=`/tasks/${task.id}/edit`)= t('views.task.list.edit')
            +buttonTo(`/tasks/${task.id}`, 'delete')(value=t('views.task.list.delete') class="btn btn-link")
